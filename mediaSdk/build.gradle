apply plugin: 'com.android.library'
//apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-kapt'
apply plugin: 'kotlin-parcelize'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion


    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        externalNativeBuild {
            cmake {
                cppFlags "-frtti -fexceptions"
                arguments "-DANDROID_ARM_NEON=TRUE"
            }
        }
        ndk {
            abiFilters "armeabi-v7a", 'arm64-v8a'
        }

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            consumerProguardFiles 'proguard-rules.pro'
        }
        debug {
            jniDebuggable true
        }
    }
    externalNativeBuild {
        cmake {
            path "CMakeLists.txt"
        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
            //是否取消动态编译
//            jni.srcDirs = []

            java {
                srcDir "src/main/java"
                srcDir "../mediaSdkRes/main/java"
            }
            res {
                srcDir "src/main/res"
                srcDir "../mediaSdkRes/src/main/res"
            }
            assets {
                srcDir "src/main/assets"
                srcDir "../mediaSdkRes/src/main/assets"
            }
        }

    }

    ndkVersion '21.0.6113669'
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'junit:junit:4.13.2'
    testImplementation 'junit:junit:4.13.2'
    api "org.jetbrains.kotlinx:kotlinx-coroutines-test:1.6.4"

    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
    api 'com.google.code.gson:gson:2.10'
    api 'com.github.bumptech.glide:glide:4.15.0'
    api 'jp.wasabeef:glide-transformations:4.3.0'
    api 'com.google.android.exoplayer:exoplayer-core:2.18.5'
    api  'androidx.core:core-ktx:1.9.0-alpha02'
    api "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.6.4"
    api "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.6.4"
    api "androidx.lifecycle:lifecycle-viewmodel-ktx:2.6.1"
    api("androidx.lifecycle:lifecycle-runtime-ktx:2.6.1")
    api 'com.tencent.tav:libpag:4.0.5.10'


}
repositories {
    mavenCentral()
}
